---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Checkout - Diagnóstico 1:1">
  <main class="min-h-dvh bg-white text-black">
    <!-- top band (premium image) -->
    <div
      class="h-36"
      style="background-image: url('/checkout-band-premium.png'); background-size: cover; background-position: center;"
    ></div>

    <div class="mx-auto -mt-20 grid max-w-5xl gap-10 px-6 pb-16 pt-10 md:grid-cols-2">
      <!-- Left column (content) -->
      <section class="order-2 md:order-1">
        <div class="rounded-md bg-white px-2 py-2 md:px-0 md:py-0">
          <div class="inline-block rounded-md bg-white p-0 shadow-sm">
            <img
              src="/checkout-zoom-v10.png?v=1"
              alt=""
              class="h-auto w-full max-w-[520px] rounded-md bg-white"
              loading="eager"
              decoding="async"
            />
          </div>

          <h1 class="mt-8 text-3xl font-extrabold tracking-tight">
            Diagnóstico Estratégico 1:1
          </h1>

          <p id="checkoutIntro" class="mt-4 text-[15px] leading-relaxed text-black/70">
            Si has llegado hasta aquí, es porque <span class="font-semibold text-black/85">no quieres seguir igual</span>.
            Probablemente ya ganas “lo suficiente”… pero aun así sientes que estás atrapado: no tienes margen real para decidir dónde o cómo vivir, ni para cambiar de trabajo, ni para tocar nada importante en tu vida.
          </p>

          <p class="mt-3 text-[15px] leading-relaxed text-black/70">
            Esta sesión es para <span class="font-semibold text-black/85">desbloquear tu situación</span>: ver con claridad qué estás sosteniendo, qué decisión estás evitando y cuál es el primer movimiento para recuperar margen sin romperlo todo.
          </p>

          <p id="checkoutIntroNewsletter" class="mt-3 hidden text-[15px] leading-relaxed text-black/70">
            Si vienes de la newsletter: aquí es donde <span class="font-semibold text-black/85">desbloqueamos tu caso</span>. En 60 minutos te digo qué estás sosteniendo, qué decisión estás evitando y cuál es tu primer movimiento para recuperar margen real.
          </p>

          <hr class="mt-8 border-black/10" />

          <h2 class="mt-8 text-lg font-extrabold tracking-tight">¿Qué es esta sesión?</h2>
          <div class="mt-4 space-y-2 text-[15px] leading-relaxed text-black/70">
            <p class="mt-4">Es una conversación de 60 minutos donde vamos a:</p>
            <ul class="mt-3 space-y-2 text-[15px] leading-relaxed text-black/70">
              <li>– Ver con claridad dónde estás <span class="font-semibold text-black/85">de verdad</span>.</li>
              <li>– Detectar qué decisiones te están frenando.</li>
              <li>– Salir con un plan simple de 30 días.</li>
            </ul>
          </div>

          <hr class="mt-8 border-black/10" />

          <h2 class="mt-8 text-lg font-extrabold tracking-tight">¿Qué incluye?</h2>
          <div class="mt-4 space-y-3 text-[15px] leading-relaxed text-black/70">
            <ul class="space-y-2 text-[15px] leading-relaxed text-black/70">
              <li>– 60 minutos 1:1 conmigo.</li>
              <li>– Análisis directo (y con honestidad).</li>
              <li>– Un siguiente paso definido.</li>
            </ul>
          </div>

          <hr class="mt-8 border-black/10" />

          <h2 class="mt-8 text-lg font-extrabold tracking-tight">¿Qué iría a continuación?</h2>
          <div class="mt-4 space-y-3 text-[15px] leading-relaxed text-black/70">
            <p>Tras la sesión te envío un resumen con las conclusiones y un audio con los pasos para que tengas el plan por escrito.</p>
            <p>A partir de ahí, puedes aplicar lo que te llevas por tu cuenta o, si encaja y estás preparado, acceder a la <span class="font-semibold text-black/85">Intervención Estratégica 60D</span>: trabajamos directamente conmigo siguiendo un plan y una estrategia personalizada para lograr <span class="font-semibold text-black/85">mayor libertad en el menor tiempo posible</span>.</p>
          </div>

          <hr class="mt-8 border-black/10" />

          <p class="mt-8 text-sm text-black/70">No estás pagando por una llamada.</p>
          <p class="mt-2 text-sm text-black/70">Estás pagando por <span class="font-semibold text-black/85">desbloquear tu situación</span>.</p>
        </div>
      </section>

      <!-- Right column (payment card) -->
      <aside class="order-1 md:order-2">
        <div class="rounded-md bg-white p-7 shadow-xl ring-1 ring-black/5">
          <div class="flex items-start justify-between">
            <div>
              <p class="text-2xl font-extrabold">€60,00 EUR</p>
              <p class="mt-1 text-xs text-black/45">Pago único</p>
            </div>
            <a href="/login" class="text-xs font-semibold text-blue-600 hover:underline">Iniciar sesión</a>
          </div>

          <form id="checkoutForm" class="mt-7 space-y-4">
            <input
              id="name"
              class="h-10 w-full rounded border border-black/15 bg-white px-3 text-sm outline-none placeholder:text-black/30"
              placeholder="Nombre"
              autocomplete="name"
              required
            />

            <input
              id="email"
              class="h-10 w-full rounded border border-black/15 bg-white px-3 text-sm outline-none placeholder:text-black/30"
              placeholder="Dirección de correo electrónico"
              autocomplete="email"
              type="email"
              required
            />

            <p class="text-xs text-black/45">No es una suscripción. No hay pagos ocultos.</p>
            <p class="mt-2 text-xs text-black/55">Tras el pago, te enviaremos el acceso por email (revisa Spam).</p>

            <button
              id="payBtn"
              type="submit"
              class="mt-2 inline-flex h-11 w-full items-center justify-center rounded bg-slate-700 px-5 text-sm font-semibold text-white"
            >
              👉 Pagar con Stripe
            </button>

            <p id="payStatus" class="text-xs text-black/55"></p>
          </form>

          <script>
            const form = document.getElementById('checkoutForm');
            const statusEl = document.getElementById('payStatus');
            const btn = document.getElementById('payBtn');

            const params = new URLSearchParams(window.location.search);
            if (params.get('payment') === 'cancelled') {
              statusEl.textContent = '⚠️ El pago no se ha realizado. Vuelve a intentarlo.';
            }

            // Personalize copy when coming from newsletter
            if (params.get('src') === 'newsletter') {
              const intro = document.getElementById('checkoutIntro');
              const introNl = document.getElementById('checkoutIntroNewsletter');
              if (intro) intro.classList.add('hidden');
              if (introNl) introNl.classList.remove('hidden');
            }

            form?.addEventListener('submit', async (e) => {
              e.preventDefault();
              try { window.clarity && window.clarity('event', 'CHECKOUT_PAY_CLICK'); } catch (e) {}
              statusEl.textContent = 'Redirigiendo a Stripe…';
              btn.disabled = true;
              btn.classList.add('opacity-60');

              const name = document.getElementById('name').value;
              const email = document.getElementById('email').value;

              try {
                const res = await fetch('https://fluirplus-webhook.gonzalo-pareja.workers.dev/stripe/create-checkout-session', {
                  method: 'POST',
                  headers: { 'content-type': 'application/json' },
                  body: JSON.stringify({ name, email }),
                });
                const data = await res.json().catch(() => ({}));
                if (!res.ok || !data?.url) throw new Error('stripe_session_failed');
                window.location.href = data.url;
              } catch (err) {
                btn.disabled = false;
                btn.classList.remove('opacity-60');
                statusEl.textContent = 'No pude iniciar el pago. Intenta de nuevo en 30s.';
              }
            });
          </script>
        </div>
      </aside>
    </div>
  </main>
</BaseLayout>
